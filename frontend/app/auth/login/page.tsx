'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { ArrowRight, LockKeyhole, ShieldCheck } from 'lucide-react'
import { useAuthStore } from '@/lib/auth-store'
import Link from 'next/link'

export default function LoginPage() {
  const router = useRouter(); const { login, isLoading, error } = useAuthStore(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [totpCode, setTotpCode] = useState(''); const requiresMfa = false; const [localError, setLocalError] = useState('')
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setLocalError(''); if (!email || !password) { setLocalError('Please fill in all fields'); return } try { await login(email.trim(), password); router.push('/dashboard') } catch (err: any) { const detail = err?.response?.data?.detail; setLocalError(typeof detail === 'string' && /mfa|totp|two.factor/i.test(detail) ? detail : 'Invalid email or password.') } }
  return <main className="soc-shell grid min-h-screen lg:grid-cols-[1.05fr_.95fr]"><section className="hidden flex-col justify-between border-r border-white/[.08] p-12 lg:flex"><Link href="/" className="flex items-center gap-3 text-sm font-bold tracking-wide text-slate-100"><span className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-300"><ShieldCheck className="size-5" /></span>ADAPTIVE ZERO TRUST AI</Link><div className="max-w-xl"><p className="eyebrow text-cyan-300">Secure access layer</p><h1 className="mt-5 text-6xl font-semibold leading-[1.02] tracking-tight text-slate-50">Trust every request.<br /><span className="text-cyan-300">Verify every signal.</span></h1><p className="mt-6 max-w-lg text-base leading-7 text-slate-400">A focused operations workspace for continuous authentication, adaptive policy enforcement, and explainable risk intelligence.</p></div><p className="text-xs text-slate-600">Protected workspace · Continuous verification enabled</p></section><section className="flex items-center justify-center px-5 py-10 sm:px-10"><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><Link href="/" className="flex items-center gap-3 text-sm font-bold text-slate-100"><ShieldCheck className="size-5 text-cyan-300" />ADAPTIVE ZERO TRUST AI</Link></div><div className="soc-panel p-6 sm:p-9"><div className="mb-8"><div className="mb-5 flex size-11 items-center justify-center rounded-xl border border-cyan-300/20 bg-cyan-300/10 text-cyan-300"><LockKeyhole className="size-5" /></div><p className="eyebrow">Operator authentication</p><h2 className="mt-2 text-3xl font-semibold tracking-tight text-slate-50">Welcome back</h2><p className="mt-2 text-sm leading-6 text-slate-400">Sign in to open your security command center.</p></div><form onSubmit={submit} className="space-y-5"><label className="block text-sm font-medium text-slate-200">Email address<input type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="you@company.com" className="input mt-2" disabled={isLoading} required /></label><label className="block text-sm font-medium text-slate-200">Password<input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Enter your password" className="input mt-2" disabled={isLoading} required /></label>{requiresMfa && <label className="block text-sm font-medium text-slate-200">MFA code<input type="text" inputMode="numeric" value={totpCode} onChange={e => setTotpCode(e.target.value.replace(/\D/g, '').slice(0, 8))} placeholder="123456" className="input mt-2" disabled={isLoading} required /></label>}<div className="flex justify-end"><Link href="/auth/forgot-password" className="text-xs font-semibold text-cyan-300 hover:text-cyan-200">Forgot password?</Link></div>{(localError || error) && <div className="rounded-xl border border-rose-400/30 bg-rose-400/10 p-3 text-sm text-rose-200" role="alert">{localError || error}</div>}<button type="submit" disabled={isLoading} className="btn btn-primary w-full">{isLoading ? 'Signing in...' : 'Sign in'}<ArrowRight className="size-4" /></button></form><div className="my-6 border-t border-white/[.08]" /><Link href="/auth/register" className="btn btn-secondary w-full">Create an account</Link></div></div></section></main>
}
